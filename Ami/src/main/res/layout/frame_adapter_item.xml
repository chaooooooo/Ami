<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="@layout/drawer_launcher"
    android:background="@color/common_background_color"
    android:orientation="horizontal">

    <LinearLayout
        android:orientation="vertical"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="match_parent">
        <TextView
            android:id="@+id/frame_adapter_item_name"
            android:textSize="14sp"
            android:textColor="@android:color/primary_text_dark"
            android:lines="1"
            android:ellipsize="end"
            tools:text="Android"
            android:textColorHint="#888888"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/frame_adapter_item_class_name"
            android:textSize="12sp"
            android:textColor="@android:color/secondary_text_dark"
            android:ellipsize="end"
            android:visibility="gone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/frame_adapter_item_value"
            android:textSize="12sp"
            android:textColor="@android:color/primary_text_dark"
            android:lines="1"
            android:ellipsize="end"
            tools:text="android"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </LinearLayout>

    <ImageView
        android:id="@+id/spreader"
        android:src="@drawable/ami_frame_item_shrink"
        android:scaleType="fitStart"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:contentDescription="@string/empty_image_description"
        android:layout_width="wrap_content"
        android:layout_height="match_parent" />
</LinearLayout>